# Public site metadata. Copy this file to `.env` and adjust the values.
# Only variables prefixed with VITE_ are exposed to the client bundle.

VITE_SITE_URL=https://advaithy.github.io/3d-portfolio-site
VITE_CONTACT_EMAIL=hello@example.com
